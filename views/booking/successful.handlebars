{{!-- <p>Home > Courses > Book Now > Booking Completion</p> --}}

<link rel="stylesheet" href="/css/booking/confirmation.css">
<div class="container mt-3 rounded-2 border">
	{{#if booking}}
	<h5 class="mt-4 mx-auto">Your class has been booked successfully!</h5>
		<div>ID: {{booking.id}}</div>
		<div class="row mb-2 w-50 mx-auto">
			{{!-- <div class="col-sm-6"> --}}
				<dl class="row mx-auto">
					<dt class="col-sm-3">Details:</dt>
					<dd class="col-sm-9">
						<span></span>{{booking.Class.date}}, {{booking.Class.time}}<br>
						<span></span>{{booking.Course.level}} | {{booking.Course.title}}
						
					</dd>

				</dl>
				<a class="btn btn-info" href="/account/bookings">View Booking</a>
				{{!-- <div class="row">
					<div class="col">
						<div class="details">Details:</div>
					</div>
					<div class="col">


						<br>
						<i class="text-dark">Course ID:</i>
						<p class="text-primary"> {{booking.CourseId}}</p>

						<i class="text-dark">Class ID:</i>
						<p class="text-primary"> {{booking.ClassId}}</p>

						<i class="text-dark">Date:</i>
						<p class="text-primary">{{booking.Class.date}}</p>
					</div> --}}
					{{!--
					<div class="col">
						<u class="information">Full Information:</u>
						<br>
						<i class="text-dark">Course Name:</i>
						<p class="text-primary">{{booking.Course.title}}</p>

						<i class="text-dark">Course Description:</i>
						<p class="text-primary">{{booking.Course.description}}</p>

						<i class="text-dark">Level:</i>
						<p class="text-primary">{{booking.Course.level}}</p>

						<i class="text-dark">Price of the course:</i>
						<p class="text-primary">{{booking.Course.price}}</p>

						<i class="text-dark">Number of Pax selected:</i>
						<p class="text-primary">{{booking.Class.max_pax}}</p>
					</div> --}}
					<br>
				</div>
			</div>
		</div>
	{{else}}
	<h3>No Classes has been Booked Successfully</h3>
	{{/if}}
</div>